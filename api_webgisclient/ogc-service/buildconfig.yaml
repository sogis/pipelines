apiVersion: v1
kind: Template
metadata:
  name: build-ogc-service
  annotations:
    openshift.io/display-name: "Buildconfig for ogc-service"
    description: >-
      "Creates BuildConfig for ogc-service"
parameters:
  - description: tag of base image
    name: BASETAG
  - description: tag of deploy image
    name: DEPLOYTAG
objects:
- apiVersion: v1
  kind: BuildConfig
  metadata:
    annotations:
    labels:
      service: ogc-service
      app: gdi
    name: ogc-service
  spec:
    output:
      to:
        kind: ImageStreamTag
        name: ogc-service:${DEPLOYTAG}
    source:
      contextDir: ogc-service
      git:
        ref: master
        uri: https://github.com/sogis/qwc_services.git
      type: Git
    strategy:
      dockerStrategy:
        from:
          kind: DockerImage
          name: sourcepole/qwc-ogc-service:${BASETAG} 
        noCache: true
      type: Docker
    resources:
      limits:
        cpu: "1000m"
        memory: "4096Mi"
      requests:
        cpu: "250m"
        memory: "1024Mi"
